{
  "agent": {
    "terminal": {
      "enabled": true,
      "allowedCommands": ["git add", "git commit", "git push", "git status", "git log", "npm", "yarn", "gh pr create", "gh pr view"],
      "blockedCommands": ["rm", "sudo", "curl", "wget"],
      "requireConfirmation": false
    },
    "securityLevel": "low"
  }
}